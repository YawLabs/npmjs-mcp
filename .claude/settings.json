{
  "$schema": "https://json.schemastore.org/claude-code-settings.json",
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "shell": "bash",
            "command": "command=\"$(jq -r '.tool_input.command // empty')\"; [[ \"$command\" =~ (^|[^[:alnum:]_-])git[[:space:]]+commit($|[^[:alnum:]_-]) ]] || exit 0; npx tsc --noEmit >&2 || exit 2",
            "timeout": 60,
            "statusMessage": "Type-checking before commit"
          }
        ]
      }
    ]
  }
}
